import React from "react"


function Tags(props) {
    //console.log(props.tag.length)

    return(
        <p className="apartment__tags--name">{props.name}</p>  
        
    )  
}

export default Tags